%h1 Login

This page exists <b>merely for development and testing</b>.
In production authentication is handled by apache-httpd.

= form_tag do
  %fieldset
    %legend Log-In:
    %div
      %label uID:
      =text_field_tag :uid, params[:uid]
    %div
      = submit_tag "Login", :id=> "login"

